The migration failed because table invoice_0 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_1 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_2 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_3 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_4 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_5 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_6 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_7 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_8 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_9 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_10 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_11 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_12 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_13 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_14 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_15 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_16 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_17 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_18 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_19 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_20 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_21 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_22 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_23 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_24 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_25 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_26 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_27 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_28 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_29 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_30 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_31 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_32 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_33 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_34 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_0 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_1 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_2 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_3 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_4 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_5 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_6 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_7 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_8 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

---

The migration failed because table invoice_9 lacked the created_at column. The fix changes the database schema and the rollback script names the exact migration file.

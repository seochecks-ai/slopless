# Release 0.2.7

## Summary

Prepared Slopless `0.2.7` for npm release because `0.2.6` is already published and the skill installer is not available from npm at that version.

## Decisions Made

- Bumped `package.json`, `package-lock.json`, and CLI `--version` to `0.2.7`.
- Kept this as a patch release because it adds user-facing documentation and agent skill installation without changing rule behavior.

## Key Files For Context

- `package.json`
- `package-lock.json`
- `src/cli.ts`
- `skills/slopless/SKILL.md`

## Next Steps

- Validate, push `development`, merge to `main`, publish to npm, and verify npm installation.

# Allow `unrs` in cspell dictionary

## Summary

cspell flagged `unrs` (in `pnpm-workspace.yaml` from the previous commit) as unknown. Added it to the cspell allow list. Verified `pnpm run validate` passes the full pipeline locally.
